<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>《C程序设计语言》-引言、导言</title>
    <url>/2021/07/20/%E3%80%8AC%E7%A8%8B%E5%BA%8F%E8%AE%BE%E8%AE%A1%E8%AF%AD%E8%A8%80%E3%80%8B-1/</url>
    <content><![CDATA[<h4 id="引言"><a class="markdownIt-Anchor" href="#引言"></a> 引言</h4>
<p>C语言是一种通用的程序设计语言。<br />
它同<strong>UNIX系统</strong>之间具有非常密切的联系——C语言是在UNIX系统上开发的，并且，无论是UNIX系统本身还是其上运行的大部分程序，都是用C语言编写的。<br />
但是，C语言并不受限于任何一种操作系统或机器。由于它很适合用来编写编译器和操作系统，因此被称为**“系统编程语言”**，但它同样适合于编写不同领域中的大多数程序。</p>
<p>C语言的很多重要概念来源于由Martin Richards开发的BCPL语言。BCPL语言对C语言的影响间接地来自于B语言，它是Ken Thompson为第一个UNIX系统而于1970年在DEC PDP-7计算机上开发的。</p>
<a id="more"></a>
<p>BCPL语言和B语言都是“无类型”的语言。相比较而言，C语言提供了很多数据类型。<br />
其基本类型包括<strong>字符</strong>、具有<strong>多种长度</strong>的<strong>整型</strong>和<strong>浮点数</strong>等。另外，还有通过<strong>指针</strong>、<strong>数组</strong>、<strong>结构</strong>和<strong>联合</strong>派生的各种数据类型。<br />
表达式由运算符和操作数组成。<strong>任何一个表达式</strong>，包括赋值表达式或函数调用表达式，<strong>都可以是一个语句</strong>。<br />
<strong>指针提供了与具体机器无关的地址算术运算</strong>。</p>
<p>C语言为实现结构良好的程序提供了<strong>基本的控制流结构</strong>：**语句组、条件判断（if-else）、多路选择（switch）、终止测试在顶部的循环（while、for）、终止测试在底部的循环（do）、提前跳出循环（break）**等。</p>
<p>函数不能嵌套定义，但可以嵌套调用。任何函数都可以<strong>递归</strong>调用。<br />
一个C语言程序的不同函数可以出现在多个单独编译的不同源文件中。<br />
变量可以只在函数内部有效（局部变量），也可以在函数外部但仅在一个源文件中有效，还可以在整个程序中都有效。<br />
局部变量通常是“自动的”，即在每次函数调用时重新创建。</p>
<p><strong>编译的预处理阶段</strong>将对程序文本进行宏替换、包含其它源文件以及进行条件编译。</p>
<p>C语言是一种相对“低级”的语言。<strong>这种说法并没有什么贬义</strong>，它仅仅意味着C语言可以处理大部分计算机能够处理的对象，比如字符、数字和<strong>地址</strong>。这些对象可以通过具体机器实现的算术运算符和逻辑运算符组合在一起并移动。</p>
<p>C语言<strong>不提供直接处理诸如字符串、集合、列表或数组等复合对象的操作</strong>。虽然可以将整个结构作为一个单元进行拷贝，但C语言没有处理整个数组或字符串的操作。<br />
除了由函数的局部变量提供的静态定义和堆栈外，C语言<strong>没有定义任何存储器分配工具，也不提供堆和无用内存回收工具</strong>。<br />
最后，C语言本身<strong>没有提供输入／输出功能，没有READ或WRITE语句，也没有内置的文件访问方法</strong>。<br />
<strong>所有这些高层的机制必须由显式调用的函数提供。C语言的大部分实现已合理地包含了这些函数的标准集合。</strong><br />
类似地，C语言<strong>只提供简单的单线程控制流，即测试、循环、分组和子程序</strong>，它<strong>不提供多道程序设计、并行操作、同步和协同例程</strong>。</p>
<p>尽管缺少其中的某些特性看起来好像是一个严重不足（比如必须通过调用函数来比较两个字符串），但是<strong>把语言保持在一个适度的规模会有很多益处。由于C语言相对较小，因此可以用比较小的篇幅将它描述出来，这样也很容易学会。程序员有理由期望了解、理解并真正彻底地使用完整的语言</strong>。</p>
<p><strong>很多年来，C语言的定义就是《The C Programming Language》第1版中的参考手册</strong>。<br />
1983年，<strong>美国国家标准协会（ANSI）<strong>成立了一个委员会以制定一个现代的、全面的C语言定义。最后的结果就是1988年完成的ANSI标准，即</strong>“ANSI C”</strong>。<br />
该标准的大部分特性已被当前的编译器所支持。</p>
<p><strong>这个标准是基于以前的参考手册制定的，语言本身只做了相对较少的改动。这个标准的目的之一就是确保现有的程序仍然有效，或者当程序无效时，编译器会对新的定义发出警告信息。</strong><br />
<strong>对大部分程序员来说，最重要的变化是函数声明和函数定义的新语法</strong>。现在，函数声明中可以包含描述函数实际参数的信息；相应地，定义的语法也做了改变。这些附加的信息使编译器很容易检测到因参数不匹配而导致的错误。根据我们的经验，这个扩充对语言非常有用。<br />
新标准还对语言做了一些细微的改进：将广泛使用的结构赋值和杖举定义为语言的正式组成部分；可以进行单精度的浮点运算；明确定义了算术运算的属性，特别是无符号类型的运算；对预处理器进行了更详尽的说明。这些改进对大多数程序员的影响比较小。<br />
该标准的第二个重要贡献是为C语言定义了一个<strong>函数库</strong>。它描述了诸如访问操作系统（如读写文件）、格式化输入／输出、内存分配和字符串操作等类似的很多函数。该标准还定义了一系列的<strong>标准头文件</strong>，它们为访问函数声明和数据类型声明提供了统一的方法。这就确保了使用这个函数库与宿主系统进行交互时程序之间具有<strong>兼容</strong>的行为。<strong>该函数库很大程度上与UNIX系统的“标准 I/O 库”相似</strong>。这个函数库已在本书的第1版中进行了描述，很多系统中都使用了它，这一点对大部分程序员来说，不会感觉到有很大的变化。</p>
<p>由于大多数计算机本身就直接支持C语言提供的数据类型和控制结构，因此只需要一个很小的运行时库就可以实现自包含程序。由于程序只能够显式地调用标准库中的函数，因此在不需要的情况下就可以避免对这些函数的调用。除了其中隐藏的一些操作系统细节外，大部分库函数可以用C语言编写，并可以移植。<br />
尽管C语言能够运行在大部分的计算机上，但它同具体的机器结构无关。只要稍加用心就可以编写出可移植的程序，即可以不加修改地运行于多种硬件上。ANSI标准明确地提出了<strong>可移植性</strong>问题，并预设了一个常量的集合，借以描述运行程序的机器的特性。</p>
<p><strong>C语言不是一种强类型的语言，但随着它的发展，其类型检查机制已经得到了加强</strong>。尽管C语言的最初定义不赞成在指针和整型变量之间交换值，但并没有禁止，不过现在已经不允许这种做法了。<br />
ANSI标准要求对变量进行正确的声明和显式的强制类型转换，这在某些较完善的编译器中已经得到了实现。新的函数声明方式是另一个得到改进的地方：编译器将对大部分的数据类型错误发出警告，并且不自动执行不兼容数据类型之间的类型转换。<br />
不过，<strong>C语言保持了其初始的设计思想，即程序员了解他们在做什么，惟一的要求是程序员要明确地表达他们的意图</strong>。</p>
<p>同任何其它语言一样，C语言也有不完美的地方。某些运算符的优先级是不正确的；语法的某些部分可以进一步优化。尽管如此，对于大量的程序设计应用来说，C语言是一种公认的非常高效的、表示能力很强的语言。</p>
<p>本书是按照下列结构编排的：<br />
第1章将对C语言的核心部分进行简要介绍。其目的是让读者能尽快开始编写C语言程序，因为我们深信，<strong>实际编写程序才是学习一种新语言的好方法</strong>。这部分内容的介绍假定读者对程序设计的基本元素有一定的了解。我们在这部分内容中没有解释计算机、编译等概念，也没有解释诸如n = n + 2这样的表达式。我们将尽量在合适的地方介绍一些实用的程序设计技术，但是，<strong>本书的中心目的并不是介绍数据结构和算法。在篇幅有限的情况下，我们将专注于讲解语言本身</strong>。</p>
<p>第2章到第6章将更详细地讨论C语言的各种特性，所采用的方式将比第1章更加<strong>形式化</strong>一些。其中的<strong>重点将放在一些完整的程序例子上，而并不仅仅只是一些孤立的程序段</strong>。<br />
第2章介绍基本的数据类型、运算符和表达式。<br />
第3章介绍控制流，如if-else、switch、while和for等。<br />
第4章介绍函数和程序结构——外部变量、作用域规则和多源文件等，同时还会讲述一些预处理器的知识。<br />
第5章介绍指针和地址运算。<br />
第6章介绍结构和联合。</p>
<p>第7章介绍标准库。标准库提供了一个与操作系统交互的公用接口。这个函数库是由ANSI标准定义的，这就意味着所有支持C语言的机器都会支持它，因此，使用这个库执行输入／输出或其它访问操作系统的操作的程序可以不加修改地运行在不同机器上。</p>
<p>第8章介绍C语言程序和UNIX操作系统之间的接口，我们将把重点放在输入／输出、文件系统和存储分配上。<strong>尽管本章中的某些内容是针对UNIX系统所写的，但是使用其它系统的程序员仍然会从中获益，比如深入了解如何实现标准库以及有关可移植性方面的一些建议</strong>。</p>
<p>附录A是一个<strong>语言参考手册</strong>。虽然C语言的语法和语义的官方正式定义是ANSI标准本身，但是，<strong>ANSI标准的文档首先是写给编译器的编写者看的，因此，对程序员来说不一定最合适</strong>。本书中的参考手册采用了一种不很严格的形式，更简洁地对C语言的定义进行了介绍。<br />
附录B是对<strong>标准库</strong>的一个总结，它同样是<strong>为程序员而非编译器实现者准备的</strong>。<br />
附录C对<strong>标准C语言（ANSI C）相对最初的C语言版本所做的变更</strong>做了一个简短的小结。但是，如果有不一致或疑问的地方，标准本身和各个特定的编译器则是解释语言的最终权威。<br />
本书的最后提供了本书的索引。</p>
<h4 id="导言"><a class="markdownIt-Anchor" href="#导言"></a> 导言</h4>
<h5 id="入门"><a class="markdownIt-Anchor" href="#入门"></a> 入门</h5>
<p>UNIX</p>
<ul>
<li>编写：hello.c</li>
<li>编译：cc hello.c</li>
<li>运行：a.out</li>
</ul>
<p><code>hello.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line">main() </span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;hello, world!\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>
<p>一个C语言程序，无论其大小如何，都是由<strong>函数和变量</strong>组成的。<br />
<strong>函数中包含一些语句</strong>，以指定所要执行的计算操作；变量则用于存储计算过程中使用的值。</p>
</li>
<li>
<p>main是一个特殊的函数名——<strong>每个程序都从main函数的起点开始执行</strong>，这意味着每个程序都必须在某个位置包含一个main函数。</p>
</li>
<li>
<p>main函数通常会调用其它函数来帮助完成某些工作，被调用的函数可以是程序设计人员自己编写的，也可以来自于函数库。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br></pre></td></tr></table></figure>
<p>这条语句用于告诉编译器在本程序中包含标准输入／输出库的信息。</p>
</li>
<li>
<p>调用函数时，只需要使用函数名加上用圆括号括起来的参数表即可。<br />
函数之间进行数据交换的一种方法是调用函数时向被调用函数提供一个值（称为参数）列表。函数名后面的一对圆括号将参数列表括起来。<br />
函数中的语句用一对花括号{}括起来。</p>
</li>
<li>
<p>printf是一个用于打印输出的库函数，<strong>printf函数永远不会自动换行</strong>。故上面的程序可以改写成下列形式：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt; </span></span></span><br><span class="line">main() </span><br><span class="line">&#123; </span><br><span class="line"> 	<span class="built_in">printf</span>(<span class="string">&quot;hello, &quot;</span>); </span><br><span class="line"> 	<span class="built_in">printf</span>(<span class="string">&quot;world&quot;</span>); </span><br><span class="line"> 	<span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>); </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>
<p>在printf函数的参数中，只能用\n表示换行符。如果用程序的换行代替\n，例如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;hello, world  </span></span><br><span class="line"><span class="string">       &quot;</span>);  </span><br></pre></td></tr></table></figure>
<p>C编译器将会产生一条错误信息。</p>
</li>
<li>
<p>\n是<strong>转义字符序列</strong>，只代表一个字符。转义字符序列为表示<strong>无法输入的字符或不可见字符</strong>提供了一种通用的可扩充的机制。<br />
C语言提供的转义字符序列还包括：\t表示制表符；\b表示回退符；\&quot;表示双引号；\\表示反斜杠符本身。</p>
</li>
<li>
<p>用双引号括起来的字符序列称为<strong>字符串或字符串常量</strong>。</p>
</li>
</ol>
<h5 id="变量与算术表达式"><a class="markdownIt-Anchor" href="#变量与算术表达式"></a> 变量与算术表达式</h5>
<p><code>temperature_1.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="comment">/* 当fahr = 0, 20, ... , 300时，分别打印华氏温度与摄氏温度对照表。℃=(5/9)(℉-32)。 */</span></span><br><span class="line">main() </span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> fahr, celsius;</span><br><span class="line">    <span class="keyword">int</span> lower, upper, step;</span><br><span class="line">    </span><br><span class="line">    lower = <span class="number">0</span>;		<span class="comment">/* 温度表的下限 */</span></span><br><span class="line">    upper = <span class="number">300</span>;	<span class="comment">/* 温度表的上限 */</span></span><br><span class="line">    step = <span class="number">20</span>;		<span class="comment">/* 步长 */</span></span><br><span class="line">    fahr = lower;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (fahe &lt;= upper) &#123;</span><br><span class="line">		celsius = <span class="number">5</span> * (fahr - <span class="number">32</span>) / <span class="number">9</span>;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d\t%d\n&quot;</span>, fahr, celsius);</span><br><span class="line">        fahr = fahr + step;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>
<p>包含在/*与*/之间的字符序列称为<strong>注释</strong>，注释会被编译器忽略。<br />
程序中允许出现**空白（空格、制表符或换行符）**之处，都可以使用注释。<br />
注释可以自由地运用在程序中，使得程序更易于理解。</p>
</li>
<li>
<p>在C语言中，<strong>所有变量都必须先声明后使用</strong>。<br />
<strong>声明通常放在函数起始处，在任何可执行语句之前</strong>。<br />
声明用于说明变量的属性，它由一个类型名和一个变量表组成。</p>
</li>
<li>
<p>C语言提供的<strong>基本数据类型</strong>：</p>
<p><strong>char：字符——一个字节<br />
short：短整型<br />
int：整型<br />
long：长整型<br />
float：单精度浮点型<br />
double：双精度浮点型</strong></p>
<p><strong>这些数据类型对象的大小取决于具体的机器</strong>。<br />
另外，还<strong>存在这些基本数据类型的数组、结构、联合，指向这些类型的指针以及返回这些类型值的函教</strong>。</p>
</li>
<li>
<pre class="highlight"><code class="c">lower = <span class="hljs-number">0</span>;
upper = <span class="hljs-number">300</span>;
<span class="hljs-built_in">step</span> = <span class="hljs-number">20</span>;
fahr = lower;
&lt;!--code￼<span class="hljs-number">5</span>--&gt;

在该赋值语句中，之所以把表达式写成先乘<span class="hljs-number">5</span>然后再除以<span class="hljs-number">9</span>而不是直接写成<span class="hljs-number">5</span> / <span class="hljs-number">9</span>，其原因是在C语言及许多其它语言中，**整数除法操作将执行舍位，结果中的任何小数部分都会被舍弃**。由于<span class="hljs-number">5</span>和<span class="hljs-number">9</span>都是整数，<span class="hljs-number">5</span> / <span class="hljs-number">9</span>相除后经截取所得的结果为<span class="hljs-number">0</span>，因此这样求得的所有摄氏温度都将为<span class="hljs-number">0</span>。

</code></pre>
</li>
<li>
<p>printf是一个通用输出格式化函数。<br />
该函数的第一个参数是待打印的字符串，其中的每个百分号（%）表示其它的参数（第二个、第三个、……参数）之一进行替换的位置，并指定打印格式。<br />
printf函数的第一个参数中的各个%分别对应于第二个、第三个、……参数，它们<strong>在数目和类型上都必须匹配</strong>，否则将出现错误的结果。<br />
%d指定一个整型参数。如果在printf语句的第一个参数的%d中指明打印宽度，则打印的数字会在打印区域内右对齐。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot; %3d %6d\n&quot;</span>, fahr, celsius); </span><br></pre></td></tr></table></figure>
</li>
<li>
<p>由于我们使用的是整型算术运算，因此经计算得到的摄氏温度值不太精确，例如，与0℉对应的精确的摄氏温度应该为-17.8℃，而不是-17℃。为了得到更精确的结果，应该用浮点算术运算代替上面的整型算术运算。<br />
下面是该程序的另一种版本。</p>
</li>
</ol>
<p><code>temperature_2.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt; </span></span></span><br><span class="line"><span class="comment">/* print Fahrenheit-Celsius table for fahr = 0, 20, ..., 300; floating-point version */</span> </span><br><span class="line">main() </span><br><span class="line">&#123; </span><br><span class="line">    <span class="keyword">float</span> fahr, celsius; </span><br><span class="line">    <span class="keyword">float</span> lower, upper, step;</span><br><span class="line">     </span><br><span class="line">    lower = <span class="number">0</span>; 	<span class="comment">/* lower limit of temperatuire scale */</span> </span><br><span class="line">    upper = <span class="number">300</span>; 	<span class="comment">/* upper limit */</span> </span><br><span class="line">    step = <span class="number">20</span>; 	<span class="comment">/* step size */</span> </span><br><span class="line">    fahr = lower; </span><br><span class="line">     </span><br><span class="line">    <span class="keyword">while</span> (fahr &lt;= upper) &#123; </span><br><span class="line">    	celsius = (<span class="number">5.0</span> / <span class="number">9.0</span>) * (fahr - <span class="number">32.0</span>); </span><br><span class="line">    	<span class="built_in">printf</span>(<span class="string">&quot;%3.0f %6.1f\n&quot;</span>, fahr, celsius); </span><br><span class="line">    	fahr = fahr + step; </span><br><span class="line">    &#125; </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>
<ol>
<li>
<p>在前一个程序中，之所以不能使用5 / 9的形式，是因为按整型除法的计算规则，它们相除并舍位后得到的结果为0。但是，常数中的小数点表明该常数是一个浮点数，因此，5.0 / 9.0是两个浮点数相除，结果将不被舍位。<br />
<strong>即使浮点常量取的是整型值，在书写时最好还是为它加上一个显式的小数点，这样可以强调其浮点性质，便于阅读</strong>。<br />
如果某个算术运算符的所有操作数均为整型，则执行整型运算。但是，<strong>如果某个算术运算符有一个浮点型操作数和一个整型操作数，则在开始运算之前整型操作数将会被转换为浮点型</strong>。</p>
</li>
<li>
<p>printf中的转换说明%3.0f表明待打印的浮点数（fahr）至少占3个字符宽，且不带小数点和小数部分；%6.1f表明另一个待打印的数（celsius）至少占6个字符宽，且小数点后面有1位数字。<br />
格式说明可以省略宽度与精度，例如，%6f表示待打印的浮点数至少有6个字符宽；%.2f指定待打印的浮点数的小数点后有两位小数，但宽度没有限制；%f则仅仅要求按照浮点数打印该数。<br />
<strong>整型数、浮点数格式说明总结</strong>：</p>
<ul>
<li>%d ：按照十进制整型数打印</li>
<li>%6d：按照十进制整型数打印，至少6个字符宽</li>
<li>%f：按照浮点数打印</li>
<li>%6f：按照浮点数打印，至少6个字符宽</li>
<li>%.2f：按照浮点数打印，小数点后有两位小数</li>
<li>%6.2f：按照浮点数打印，至少6个字符宽，小数点后有两位小数</li>
</ul>
<p><strong>printf函数还支持下列格式说明</strong>：</p>
<ul>
<li>%o：表示八进制数</li>
<li>%x：表示十六进制数</li>
<li>%c：表示字符</li>
<li>%s：表示字符串</li>
<li>%%：表示百分号（%）本身</li>
</ul>
</li>
</ol>
<h5 id="for语句"><a class="markdownIt-Anchor" href="#for语句"></a> for语句</h5>
<p><code>temperature_3.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt; </span></span></span><br><span class="line"><span class="comment">/* 打印华氏温度—摄氏温度对照表 */</span></span><br><span class="line">main()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> fahr;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (fahr = <span class="number">0</span>; fahr &lt;= <span class="number">300</span>; fahr = fahr + <span class="number">20</span>)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%3d %6.1f\n&quot;</span>, fahr, (<span class="number">5.0</span> / <span class="number">9.0</span>) * (fahr - <span class="number">32</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>
<p>对于某个特定任务我们可以采用多种方法来编写程序。temperature_3.c与temperature_2.c执行结果相同，但程序本身有所不同。<br />
它去掉了大部分变量，而只使用了一个int类型的变量fahr。在新引入的for语句中，温度的下限、上限和步长都是常量，而计算摄氏温度的表达式现在变成了printf函数的第三个参数，它不再是一个单独的赋值语句。<br />
<strong>C语言中的一个通用规则：在允许使用某种类型变量值的任何场合，都可以使用该类型的更复杂的表达式</strong>。因为printf函数的第三个参数必须是与%6.1f匹配的浮点值，所以可以在此处使用任何浮点表达式。</p>
</li>
<li>
<p><strong>for语句是一种循环语句，它是对while语句的推广</strong>，与while语句比较操作更直观。<br />
圆括号中共包含3个部分，各部分之间用分号隔开：</p>
<ul>
<li>第一部分是初始化部分，仅在进入循环前执行一次。</li>
<li>第二部分是控制循环的测试或条件部分，循环控制将对该条件求值。</li>
<li>如果条件值为真（true），则执行循环体。此后将执行第三部分增加一个步长，并再次对条件求值。</li>
<li>如果计算得到的条件值为假（false），循环将终止执行。</li>
</ul>
<p><strong>初始化部分（第一部分）</strong>、**条件部分（第二部分）<strong>与</strong>增加步长部分（第三部分）**都可以是任何表达式。<br />
与while语句一样，for循环语句的循环体可以只有一条语句，也可以是用花括号括起来的一组语句。</p>
</li>
<li>
<p>在实际编程过程中，可以选择while与for中的任意一种循环语句，<strong>主要要看使用哪一种更清晰</strong>。<br />
<strong>for语句比较适合初始化和增加步长都是单条语句并且逻辑相关的情形，因为它将循环控制语句集中放在一起，且比while语句更紧凑</strong>。</p>
</li>
</ol>
<h5 id="符号常量"><a class="markdownIt-Anchor" href="#符号常量"></a> 符号常量</h5>
<p><code>temperature_4.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt; </span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOWER 0 	<span class="comment">/* lower limit of table */</span> </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> UPPER 300 	<span class="comment">/* upper limit */</span> </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> STEP 20		<span class="comment">/* step size */</span> </span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* print Fahrenheit-Celsius table */</span> </span><br><span class="line">main() </span><br><span class="line">&#123; </span><br><span class="line">	<span class="keyword">int</span> fahr; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">for</span> (fahr = LOWER; fahr &lt;= UPPER; fahr = fahr + STEP) </span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">&quot;%3d %6.1f\n&quot;</span>, fahr, (<span class="number">5.0</span> / <span class="number">9.0</span>)*(fahr - <span class="number">32</span>)); </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>
<ol>
<li>
<p>在程序中使用300、20等类似的“幻数”（<strong>在C语言中，把直接使用的常数叫做幻数</strong>）并不是一个好习惯，它们几乎无法向以后阅读该程序的人提供什么信息，而且使程序的修改变得更加困难。处理这种幻数的一种方法是赋予它们有意义的名字。</p>
</li>
<li>
<p><strong>#define</strong>指令可以把符号名（或称为符号常量）定义为一个特定的字符串：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#define 名字 替换文本</span><br></pre></td></tr></table></figure>
<p>在该定义之后，程序中出现的所有在#define中定义的名字（既没有用引号引起来，也不是其它名字的一部分）都将用相应的替换文本替换。其中，名字与普通变量名的形式相同：它们都是以字母打头的字母和数字序列；替换文本可以是任何字符序列，而不仅限于数字。<br />
LOWER、UPPER与STEP都是<strong>符号常量</strong>，而非变量，因此<strong>不需要出现在声明中</strong>。<br />
<strong>符号常量名通常用大写字母拼写，这样可以很容易与用小写字母拼写的变量名相区别</strong>。<br />
注意，<strong>#define指令行的末尾没有分号</strong>。</p>
</li>
</ol>
<h5 id="字符输入输出"><a class="markdownIt-Anchor" href="#字符输入输出"></a> 字符输入/输出</h5>
<p>标准库提供的输入／输出模型非常简单。无论文本从何处输入，输出到何处，其输入／输出都是按照字符流（<strong>流：随着时间的推移，字符是顺序生成或消耗的</strong>）的方式处理。<br />
文本流是由多行字符构成的字符序列，而每行字符则由0个或多个字符组成，行末是一个换行符。<br />
标准库负责使每个输入／输出流都能够遵守这一<strong>模型</strong>。使用标准库的C语言程序员不必关心在程序之外这些行是如何表示的。</p>
<p>标准库提供了一次读／写一个字符的函数，其中最简单的是<strong>getchar</strong>和<strong>putchar</strong>两个函数。<br />
每次调用时，getchar函数从文本流中读入下一个输入字符，并将其作为结果值返回。例如，在执行语句c = getchar() 之后，变量c中将包含输入流中的下一个字符。这种字符通常是通过键盘输入的。<br />
每次调用putchar函数时将打印一个字符。例如，语句putchar() 将把整型变量c的内容以字符的形式打印出来。打印结果通常是显示在屏幕上的。<br />
<strong>putchar与printf这两个函数可以交替调用，输出的次序与调用的次序一致</strong>。</p>
<h6 id="文件复制"><a class="markdownIt-Anchor" href="#文件复制"></a> 文件复制</h6>
<p><code>file_copy_1.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="comment">/* copy input to output */</span></span><br><span class="line">main()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> c;</span><br><span class="line">    </span><br><span class="line">    c = getchar();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (c != EOF) &#123;</span><br><span class="line">        <span class="built_in">putchar</span>(c);</span><br><span class="line">        c = getchar();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>对于解决<strong>如何区分文件中有效数据与输入结束符</strong>的问题，C语言采取的解决方法是：<br />
在没有输入时，getchar函数将返回一个特殊值，这个特殊值与任何实际字符都不同。这个值称为<strong>EOF（end of file，文件结束）</strong>。<br />
EOF定义在头文件中，是个整型数，<strong>其具体数值是什么并不重要</strong>，只要它与任何char类型的值都不相同即可。这里使用符号常量，可以确保程序不需要依赖于其对应的任何特定的数值。</li>
<li><strong>字符在键盘、屏幕或其它的任何地方无论以什么形式表现，它在机器内部都是以位模式存储的。char类型专门用于存储这种字符型数据，当然任何整型（int）也可以用于存储字符型数据</strong>。<br />
我们在声明变量c的时候，必须让它大到足以存放getchar函数返回的任何值。这里之所以不把c声明成char类型，是因为它必须足够大，除了能存储任何可能的字符外还要能存储文件结束符EOF。因此，我们将c声明成int类型。</li>
<li>在C语言中，类似于c = getchar() 之类的赋值操作是一个表达式，并且具有一个值，即赋值后左边变量保存的值。也就是说，赋值可以作为更大的表达式的一部分出现。如果将为c赋值的操作放在while循环语句的测试部分中，上述字符复制程序便可以精炼成下列形式。</li>
</ol>
<p><code>file_copy_2.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="comment">/* copy input to output */</span></span><br><span class="line">main()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> c;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> ((c = getchar()) != EOF)</span><br><span class="line">        <span class="built_in">putchar</span>(c);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>这段程序将输入集中化，getchar函数在程序中只出现了一次，这样就缩短了程序，整个程序看起来更紧凑。<strong>习惯这种风格后，读者就会发现按照这种方式编写的程序更易阅读。 我们经常会看到这种风格。（不过，如果我们过多地使用这种类型的复杂语句，编写的程序可能会很难理解，应尽量避免这种情况。）</strong></li>
<li>对while语句的条件部分来说，赋值表达式两边的圆括号不能省略。<br />
<strong>不等于运算符!=的优先级比赋值运算符=的优先级要高</strong>，这样，在不使用圆括号的情况下关系测试!=将在赋值=操作之前执行。<br />
因此语句c = getchar() != EOF等价于语句c = (getchar() != EOF)，该语句执行后，c的值将被置为0或1（取决于调用getchar函数时是否碰到文件结束标志），这并不是我们所希望的结果。</li>
</ol>
<h6 id="字符计数"><a class="markdownIt-Anchor" href="#字符计数"></a> 字符计数</h6>
<p><code>character_count_1.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt; </span></span></span><br><span class="line"><span class="comment">/* count characters in input */</span> </span><br><span class="line">main() </span><br><span class="line">&#123; </span><br><span class="line">	<span class="keyword">long</span> nc;</span><br><span class="line">    </span><br><span class="line">	nc = <span class="number">0</span>; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">while</span> (getchar() != EOF) </span><br><span class="line">		++nc; </span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;%ld\n&quot;</span>, nc); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>
<p><strong>long整型数（长整型）至少要占用32位存储单元。在某些机器上int与long类型的长度相同，但在一些机器上，int类型的值可能只有16位存储单元的长度（最大值为32767）</strong>，这样，相当小的输入都可能使 int 类型的计数变量溢出。</p>
</li>
<li>
<pre class="highlight"><code class="c">++nc;
&lt;!--code￼<span class="hljs-number">14</span>--&gt;

</code></pre>
</li>
<li>
<p>对于float与double类型，printf函数都使用%f进行说明。<br />
%.0f强制不打印小数点和小数部分，因此小数部分的位数为 0。</p>
</li>
<li>
<p>在该程序段中，for循环语句的循环体是空的，这是因为所有工作都在测试（条件）部分与增加步长部分完成了。但C语言的语法规则要求for循环语句必须有一个循环体，因此用单独的分号代替。<br />
<strong>单独的分号称为空语句</strong>，它正好能满足for语句的这一要求。<strong>把它单独放在一行是为了更加醒目</strong>。</p>
</li>
<li>
<p>如果输入中不包含字符，那么，在第一次调用getchar函数的时候，while语句或for语句中的条件测试从一开始就为假，程序的执行结果将为0，这也是正确的结果。这一点很重要。<br />
while语句与for语句的优点之一就是在执行循环体之前就对条件进行测试，如果条件不满足，则不执行循环体，这就可能出现循环体一次都不执行的情况（一种<strong>边界条件</strong>）。在出现边界条件时，while语句与for语句有助于确保程序执行合理的操作。</p>
</li>
</ol>
<h6 id="行计数"><a class="markdownIt-Anchor" href="#行计数"></a> 行计数</h6>
<p><code>line_count.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;  </span></span></span><br><span class="line"><span class="comment">/* count lines in input */</span> </span><br><span class="line">main() </span><br><span class="line">&#123; </span><br><span class="line">	<span class="keyword">int</span> c, nl; </span><br><span class="line">    </span><br><span class="line">	nl = <span class="number">0</span>; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">while</span> ((c = getchar()) != EOF) </span><br><span class="line">		<span class="keyword">if</span> (c == <span class="string">&#x27;\n&#x27;</span>) </span><br><span class="line">			++nl; </span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, nl); </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>
<ol>
<li>
<p>if语句先测试圆括号中的条件，如果该条件为真，则执行其后的语句（或括在花括号中的一组语句）。这里再次用缩进方式表明语句之间的控制关系。</p>
</li>
<li>
<p>双等于号<mark>是C语言中表示“等于”关系的运算符。<br />
由于C语言将单等于号=作为赋值运算符，因此使用双等于号</mark>表示相等的逻辑关系，以示区分。<br />
这里提醒注意，<strong>在表示“等于”逻辑关系的时候（应该用==），C语言初学者有时会错误地写成单等于号=，即使这样误用了，其结果通常仍然是合法的表达式，因此系统不会给出警告信息</strong>。</p>
</li>
<li>
<p><strong>单引号中的字符表示一个整型值，该值等于此字符在机器字符集中对应的数值，我们称之为字符常量。因此，它只不过是小的整型数的另一种写法而已</strong>。<br />
例如，'A’是一个字符常量，在ASCII字符集中其值为65（即字符A的内部表示值为65）。<br />
当然，用’A’要比用65好，因为，'A’的意义更清楚，且与特定的字符集无关。</p>
</li>
<li>
<p>字符串常量中使用的转义字符序列也是合法的字符常量。<br />
例如，’\n’代表换行符的值，在ASCII字符集中其值为10。<br />
我们应当注意到，<strong>’\n’是单个字符，在算术表达式中它不过是一个整型数而已；而&quot;\n&quot;是一个仅包含一个字符的字符串常量</strong>。</p>
</li>
<li>
<p><strong>对char类型的总结：<br />
char类型就是小整型，占一个字节。<br />
由于char类型整型数有以下两种意义：</strong></p>
<ol>
<li><strong>数值</strong></li>
<li><strong>数值在相应字符集中对应的字符</strong></li>
</ol>
<p><strong>因此将char类型单独独立出来作为一种数据类型，称为字符类型。<br />
字符（类型）常量单独打印出来时是字符（与整型数的区别），在算术表达式中是整型数。<br />
字符串类型是字符类型和数组类型的复合类型。<br />
一个字符串常量实际上是一个字符数组，其中包含着若干个相互独立的字符常量。</strong></p>
</li>
</ol>
<h6 id="单词计数"><a class="markdownIt-Anchor" href="#单词计数"></a> 单词计数</h6>
<p><code>word_count.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt; </span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> IN 1 	<span class="comment">/* inside a word */</span> </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> OUT 0 	<span class="comment">/* outside a word */</span>  </span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* count lines, words, and characters in input */</span> </span><br><span class="line"><span class="comment">/* 这里对单词的定义比较宽松，它是任何其中不包含空白的字符序列 */</span></span><br><span class="line">main() </span><br><span class="line">&#123; </span><br><span class="line">	<span class="keyword">int</span> c, nl, nw, nc, state; </span><br><span class="line">    </span><br><span class="line">	state = OUT; </span><br><span class="line">	nl = nw = nc = <span class="number">0</span>; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">while</span> ((c = getchar()) != EOF) &#123; </span><br><span class="line">		++nc; </span><br><span class="line">		<span class="keyword">if</span> (c == <span class="string">&#x27;\n&#x27;</span>) </span><br><span class="line">			++nl; </span><br><span class="line">		<span class="keyword">if</span> (c == <span class="string">&#x27; &#x27;</span> || c == <span class="string">&#x27;\n&#x27;</span> || c = <span class="string">&#x27;\t&#x27;</span>) </span><br><span class="line">			state = OUT; </span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (state == OUT) &#123; </span><br><span class="line">			state = IN; </span><br><span class="line">			++nw; </span><br><span class="line">		&#125; </span><br><span class="line">	&#125; </span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;%d %d %d\n&quot;</span>, nl, nw, nc); </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>
<ol>
<li>
<p>state变量记录程序当前是否正位于一个单词之中，它的初值是“不在单词中”，即初值被赋为OUT。<br />
<strong>我们在这里使用了符号常量IN与OUT，而没有使用其对应的数值1与0，这样程序更易读</strong>。<br />
在较小的程序中，这种做法也许看不出有什么优势，但在较大的程序中，如果从一开始就这样做，因此而增加的一点工作量与提高程序可读性带来的好处相比是值得的。读者也会发现，<strong>如果程序中的幻数都以符号常量的形式出现，对程序进行大量修改就会相对容易得多</strong>。</p>
</li>
<li>
<pre class="highlight"><code class="c">nl = nw = nc = <span class="hljs-number">0</span>;
&lt;!--code￼<span class="hljs-number">17</span>--&gt;

</code></pre>
</li>
<li>
<p><strong>运算符||代表OR（逻辑或）</strong>，所以下列语句：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (c == <span class="string">&#x27; &#x27;</span> || c== <span class="string">&#x27;\n&#x27;</span> || c == <span class="string">&#x27;\t&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>的意义是“如果c是空格，或c是换行符，或c是制表符”。<br />
相应地，<strong>运算符&amp;&amp;代表AND（逻辑与）</strong>，它仅<strong>比||高一个优先级</strong>。<br />
<strong>由&amp;&amp;或||连接的表达式由左至右求值，并保证在求值过程中只要能够判断最终的结果为真或假，求值就立即终止</strong>。如果c是空格，则没有必要再测试它是否为换行符或制表符，这样就不必执行后面两个测试。<br />
在这里，这一点并不特别重要，但在某些更复杂的情况下这样做就有必要了。</p>
</li>
<li>
<pre class="highlight"><code class="">if (表达式) 
	语句1 
else 
	语句2
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">   if-else中的两条语句有且仅有一条语句被执行，else部分指定当if语句中的条件部分为假时所要执行的动作。</span><br><span class="line">   如果表达式的值为真，则执行语句1，否则执行语句2。这两条语句都既可以是单条语句，也可以是括在花括号内的语句序列。</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">##### 数组</span><br><span class="line"></span><br><span class="line">&#96;array.c&#96;</span><br><span class="line"></span><br><span class="line">&#96;&#96;&#96;c</span><br><span class="line">#include &lt;stdio.h&gt; </span><br><span class="line">&#x2F;* count digits, white space, others *&#x2F; </span><br><span class="line">&#x2F;* 这个程序的实用意义并不大，但我们可以通过该程序讨论C语言多方面的问题 *&#x2F;</span><br><span class="line">main() </span><br><span class="line">&#123; </span><br><span class="line">	int c, i, nwhite, nother; </span><br><span class="line">	int ndigit[10]; </span><br><span class="line">    </span><br><span class="line">	nwhite &#x3D; nother &#x3D; 0; </span><br><span class="line">    for (i &#x3D; 0; i &lt; 10; ++i) </span><br><span class="line">		ndigit[i] &#x3D; 0; </span><br><span class="line">    </span><br><span class="line">	while ((c &#x3D; getchar()) !&#x3D; EOF) </span><br><span class="line">		if (c &gt;&#x3D; &#39;0&#39; &amp;&amp; c &lt;&#x3D; &#39;9&#39;) </span><br><span class="line">			++ndigit[c-&#39;0&#39;]; </span><br><span class="line">		else if (c &#x3D;&#x3D; &#39; &#39; || c &#x3D;&#x3D; &#39;\n&#39; || c &#x3D;&#x3D; &#39;\t&#39;) </span><br><span class="line">			++nwhite; </span><br><span class="line">		else </span><br><span class="line">			++nother; </span><br><span class="line">		</span><br><span class="line">    printf(&quot;digits &#x3D;&quot;); </span><br><span class="line">	for (i &#x3D; 0; i &lt; 10; ++i) </span><br><span class="line">		printf(&quot; %d&quot;, ndigit[i]); </span><br><span class="line">	printf(&quot;, white space &#x3D; %d, others &#x3D; %d\n&quot;, nwhite, nother); </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>

</code></pre>
</li>
<li>
<pre class="highlight"><code class="c"><span class="hljs-keyword">int</span> ndigit[<span class="hljs-number">10</span>];
&lt;!--code￼<span class="hljs-number">20</span>--&gt;

用于判断c中的字符是否为数字。
如果它是数字，那么该数字对应的数值是c - <span class="hljs-string">'0'</span>只有当**<span class="hljs-string">'0'</span>、<span class="hljs-string">'1'</span>、…、<span class="hljs-string">'9'</span>具有连续递增的值**时，这种做法才可行。幸运的是，**所有的字符集都是这样的**。 
由定义可知，<span class="hljs-keyword">char</span>类型的字符是小整型，因此<span class="hljs-keyword">char</span>类型的变量和常量在算术表达式中等价于<span class="hljs-keyword">int</span>类型的变量和常量。这样做既自然又方便。
例如，c - <span class="hljs-string">'0'</span>是一个整型表达式，如果存储在c中的字符是<span class="hljs-string">'0'</span>～<span class="hljs-string">'9'</span>，其值将为<span class="hljs-number">0</span>～<span class="hljs-number">9</span>，因此可以充当数组ndigit的合法下标。

</code></pre>
</li>
<li>
<pre class="highlight"><code class="">if (条件1) 
	语句1 
else if (条件2) 
	语句2 
... 
	... 
else 
	语句n 
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">程序中经常使用这种方式表示**多路判定**。</span><br><span class="line">在这种方式中，各条件从前往后依次求值，直到满足某个条件，然后执行对应的语句部分。这部分语句执行完成后，整个语句体执行结束（其中的任何语句都可以是括在花括号中的若干条语句）。如果所有条件都不满足，则执行位于最后一个else之后的语句（如果有的话）。如果没有最后一个else及对应的语句，该语句体将不执行任何动作。</span><br><span class="line">在第一个if与最后一个else之间可以有0个或多个下列形式的语句序列：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
else if (条件)
	语句
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">   **就程序设计风格而言，我们建议读者采用上面所示的缩进格式以体现该结构的层次关系**，否则，如果每个if都比前一个else向里缩进一些距离，那么较长的判定序列就可能超出页面的右边界。</span><br><span class="line">   **switch语句提供了编写多路分支程序的另一种方式，它特别适合于判定某个整型或字符表达式是否与一个常量集合中的某个元素相匹配的情况**。</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">##### 函数</span><br><span class="line"></span><br><span class="line">&#96;function_1.c&#96;</span><br><span class="line"></span><br><span class="line">&#96;&#96;&#96;c</span><br><span class="line">#include &lt;stdio.h&gt; </span><br><span class="line"></span><br><span class="line">int power(int m, int n);</span><br><span class="line"></span><br><span class="line">&#x2F;* test power function *&#x2F; </span><br><span class="line">main() </span><br><span class="line">&#123; </span><br><span class="line">	int i; </span><br><span class="line">    </span><br><span class="line">	for (i &#x3D; 0; i &lt; 10; ++i) </span><br><span class="line">		printf(&quot;%d %d %d\n&quot;, i, power(2,i), power(-3,i)); </span><br><span class="line">	return 0; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line">&#x2F;* power: raise base to n-th power; n &gt;&#x3D; 0 *&#x2F; </span><br><span class="line">int power(int base, int n) </span><br><span class="line">&#123; </span><br><span class="line">	int i, p; </span><br><span class="line">    </span><br><span class="line">	p &#x3D; 1; </span><br><span class="line">    </span><br><span class="line">	for (i &#x3D; 1; i &lt;&#x3D; n; ++i) </span><br><span class="line">		p &#x3D; p * base; </span><br><span class="line">	return p; </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>

</code></pre>
</li>
<li>
<p>函数为<strong>计算的封装</strong>提供了一种简便的方法。使用设计正确的函数，程序员无需考虑功能是如何实现的，而只需知道它具有哪些功能就够了。<br />
在C语言中可以简单、方便、高效地使用函数。<strong>我们经常会看到在定义后仅调用了一次的短函数，这样做可以使代码段更清晰易读</strong>。</p>
</li>
<li>
<p>main 函数在下列语句中调用了两次 power 函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d %d %d\n&quot;</span>, i, power(<span class="number">2</span>, i), power(-i, <span class="number">3</span>));</span><br></pre></td></tr></table></figure>
<p><strong>函数调用</strong>时，main函数向power函数传递两个参数；在调用执行完成时，power函数向main函数返回一个格式化的整数并打印。</p>
</li>
<li>
<p>函数定义的<strong>一般形式</strong>为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">返回值类型 函数名(0个或多个参数声明) </span><br><span class="line">&#123;  </span><br><span class="line">	声明部分 </span><br><span class="line">	语句序列 </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>
<p><strong>函数定义可以以任意次序出现在一个源文件或多个源文件中，但同一函数不能分割存放在多个文件中</strong>。<br />
<strong>如果源程序分散在多个文件中，那么，在编译和加载时，就需要做更多的工作，但这是操作系统的原因，并不是语言的属性决定的</strong>。<br />
我们暂且假定将main和power这两个函数放在同一文件中，这样前面所学的有关运行C语言程序的知识仍然有效。</p>
</li>
<li>
<pre class="highlight"><code class="c"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">power</span><span class="hljs-params">(<span class="hljs-keyword">int</span> base, <span class="hljs-keyword">int</span> n)</span>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">   power**函数定义**的第一行语句声明参数的类型、名字以及该函数返回结果的类型。</span><br><span class="line">   power函数的参数使用的名字只在power函数内部有效，对其它任何函数都是不可见的：其它函数可以使用与之相同的参数名字而不会引起冲突。</span><br><span class="line">   声明在power函数中的变量i也是这样：power函数中的i与main函数中的i无关。</span><br><span class="line">   我们通常把**函数定义**中圆括号内列表中出现的变量称为**形式参数**，而把**函数调用**中与形式参数对应的值称为**实际参数**。</span><br><span class="line"></span><br><span class="line">5. power函数计算所得的结果通过**return语句**返回给main函数。**关键字return**的后面可以跟任何表达式，形式为：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<span class="hljs-keyword">return</span> 表达式</span>; 
&lt;!--code￼<span class="hljs-number">26</span>--&gt;

表明power函数有两个<span class="hljs-keyword">int</span>类型的参数，并返回一个<span class="hljs-keyword">int</span>类型的值，这种**声明**称为**函数原型**。
它必须与power函数的定义和用法一致。如果函数的定义、用法与函数原型不一致，将出现错误。
**函数原型与函数定义中的参数名不要求相同。事实上，函数原型中的参数名是可选的，这样上面的函数原型也可以写成以下形式**：

&lt;!--code￼<span class="hljs-number">27</span>--&gt;

**但是，合适的参数名能够起到很好的说明性作用，因此我们在函数原型中总是指明参数名**。

</code></pre>
</li>
<li>
<p>ANSI C同较早版本C语言之间的最大区别在于函数的声明与定义方式的不同。<br />
按照C语言的最初定义，power函数应该写成下列形式：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* power: raise base to n-th power; n &gt;= 0 */</span>  </span><br><span class="line"><span class="comment">/* (old-style version) */</span>  </span><br><span class="line">power(base, n)  </span><br><span class="line"><span class="keyword">int</span> base, n;  </span><br><span class="line">&#123;  </span><br><span class="line">    <span class="keyword">int</span> i, p;</span><br><span class="line">    </span><br><span class="line">    p = <span class="number">1</span>;  </span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">1</span>; i &lt;= n; ++i)  </span><br><span class="line">        p = p * base;  </span><br><span class="line">    <span class="keyword">return</span> p;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其中，参数名在圆括号内指定，参数类型在左花括号之前声明。如果没有声明某个参数的类型，则默认为int类型。<br />
函数体与ANSI C中形式相同。<br />
在C语言的最初定义中，可以在程序的开头按照下面这种形式声明power函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">power</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>
<p>函数声明中不允许包含参数列表，这样编译器就无法在此时检查power函数调用的合法性。 事实上，power函数在默认情况下将被假定返回int类型的值，因此整个函数的声明可以全部省略。<br />
在ANSI C中定义的函数原型语法中，编译器可以很容易检测出函数调用中参数数目和类型方面的错误。<br />
ANSI C仍然支持旧式的函数声明与定义，这样至少可以有一个过渡阶段。但我们还是强烈建议读者：<strong>在使用新式的编译器时，最好使用新式的（ANSI C）函数原型声明方式</strong>。</p>
</li>
</ol>
<h5 id="参数传值调用"><a class="markdownIt-Anchor" href="#参数传值调用"></a> 参数——传值调用</h5>
<p><code>function_2.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt; </span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">power</span><span class="params">(<span class="keyword">int</span> m, <span class="keyword">int</span> n)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* test power function */</span> </span><br><span class="line">main() </span><br><span class="line">&#123; </span><br><span class="line">	<span class="keyword">int</span> i; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i) </span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">&quot;%d %d %d\n&quot;</span>, i, power(<span class="number">2</span>,i), i, power(<span class="number">-3</span>,i), i); </span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="comment">/* power: raise base to n-th power; n &gt;= 0 */</span> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">power</span><span class="params">(<span class="keyword">int</span> base, <span class="keyword">int</span> n)</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="keyword">int</span> p; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">for</span> (p = <span class="number">1</span>; n &gt; <span class="number">0</span>; --n) </span><br><span class="line">		p = p * base; </span><br><span class="line">	<span class="keyword">return</span> p; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>
<p>在C语言中，所有函数参数都是通过”值”传递的。也就是说，传递给被调用函数的参数值存放在临时变量中，而不是存放在原来的变量中。<br />
被调用函数不能直接修改主调函数中变量的值，而只能修改其私有的临时副本的值。</p>
</li>
<li>
<p><strong>传值调用</strong>的利大于弊。<br />
在被调用函数中，参数可以看作是便于初始化的局部变量，因此额外使用的变量更少。这样程序可以更紧凑简洁。<br />
例如，上面的程序中，参数n用作临时变量，并通过随后执行的for循环语句递减，直到其值为0，这样就不需要额外引入变量i。<br />
<strong>power函数内部对n的任何操作不会影响到主调函数中n的原始参数值，即<code>function_2.c</code>中，下列语句：</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d %d %d %d %d\n&quot;</span>, i, power(<span class="number">2</span>,i), i, power(<span class="number">-3</span>,i), i);</span><br></pre></td></tr></table></figure>
<p><strong>输出的三个i的值相等。i虽然作为参数传递给power(2, i)、power(3, i)，但power函数内部对i的操作不影响printf函数中i的原始参数值</strong>。</p>
</li>
<li>
<p><strong>必要时，也可以让函数能够修改主调函数中的变量。<br />
这种情况下，调用者需要向被调用函数提供待设置值的变量的地址（从技术角度看，地址就是指向变量的指针），而被调用函数则需要将对应的参数声明为指针类型，并通过它间接访问变量</strong>。</p>
</li>
<li>
<p>如果是<strong>数组参数</strong>，<strong>情况就有所不同了</strong>。<br />
<strong>当把数组名用作参数时，传递给函数的值是数组起始元素的位置或地址——它并不复制数组元素本身。在被调用函数中，可以通过数组下标访问或修改数组元索的值</strong>。</p>
</li>
</ol>
<h5 id="字符数组"><a class="markdownIt-Anchor" href="#字符数组"></a> 字符数组</h5>
<p><code>longest_line_1.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt; </span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAXLINE 1000 		<span class="comment">/* maximum input line length */</span>  </span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getline</span><span class="params">(<span class="keyword">char</span> line[], <span class="keyword">int</span> maxline)</span></span>; </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">copy</span><span class="params">(<span class="keyword">char</span> to[], <span class="keyword">char</span> from[])</span></span>; </span><br><span class="line"></span><br><span class="line"><span class="comment">/* print the longest input line */</span> </span><br><span class="line">main() </span><br><span class="line">&#123; </span><br><span class="line">	<span class="keyword">int</span> len; 				<span class="comment">/* current line length */</span> </span><br><span class="line">	<span class="keyword">int</span> max; 				<span class="comment">/* maximum length seen so far */</span> </span><br><span class="line">	<span class="keyword">char</span> line[MAXLINE]; 	<span class="comment">/* current input line */</span> </span><br><span class="line">	<span class="keyword">char</span> longest[MAXLINE]; 	<span class="comment">/* longest line saved here */</span> </span><br><span class="line">    </span><br><span class="line">	max = <span class="number">0</span>; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">while</span> ((len = getline(line, MAXLINE)) &gt; <span class="number">0</span>) </span><br><span class="line">		<span class="keyword">if</span> (len &gt; max) &#123; </span><br><span class="line">			max = len; </span><br><span class="line">			copy(longest, line); </span><br><span class="line">		&#125; </span><br><span class="line">	<span class="keyword">if</span> (max &gt; <span class="number">0</span>) 			<span class="comment">/* there was a line */</span> </span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, longest); </span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="comment">/* getline: read a line into s, return length */</span> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getline</span><span class="params">(<span class="keyword">char</span> s[],<span class="keyword">int</span> lim)</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="keyword">int</span> c, i; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">for</span> (i=<span class="number">0</span>; i &lt; lim<span class="number">-1</span> &amp;&amp; (c=getchar()) != EOF &amp;&amp; c != <span class="string">&#x27;\n&#x27;</span>; ++i) </span><br><span class="line">		s[i] = c; </span><br><span class="line">	<span class="keyword">if</span> (c == <span class="string">&#x27;\n&#x27;</span>) &#123; </span><br><span class="line">		s[i] = c; </span><br><span class="line">		++i; </span><br><span class="line">	&#125; </span><br><span class="line">	s[i] = <span class="string">&#x27;\0&#x27;</span>; </span><br><span class="line">	<span class="keyword">return</span> i; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="comment">/* copy: copy &#x27;from&#x27; into &#x27;to&#x27;; assume to is big enough */</span> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">copy</span><span class="params">(<span class="keyword">char</span> to[], <span class="keyword">char</span> from[])</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="keyword">int</span> i; </span><br><span class="line">    </span><br><span class="line">	i = <span class="number">0</span>; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">while</span> ((to[i] = from[i]) != <span class="string">&#x27;\0&#x27;</span>) </span><br><span class="line">		++i; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>
<p>该程序读入一组文本行，并把最长的文本行打印出来。该算法的基本框架非常简单：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">while (还有未处理的行)  </span><br><span class="line">    if (该行比已处理的最长行还要长)  </span><br><span class="line">        保存该行为最长行 </span><br><span class="line">        保存该行的长度 </span><br><span class="line">    打印最长的行</span><br></pre></td></tr></table></figure>
<p>程序很自然地分成了若干片断，分别用于读入新行、测试读入的行、保存该行，其余部分则控制这一过程。因为这种划分方式比较合理，所以可以按照这种方式编写程序。<br />
首先，我们编写一个独立的函数getline，它读取输入的下一行。我们尽量保持该函数在其它场合也有用。至少getline函数应该在读到文件末尾时返回一个信号；更为有用的设计是它能够在读入文本行时返回该行的长度，而在遇到文件结束符时返回0。由于0不是有效的行长度，因此可以作为标志文件结束的返回值。每一行至少包括一个字符，只包含换行符的行，其长度为1。<br />
当发现某个新读入的行比以前读入的最长行还要长时，就需要把该行保存起来。也就是说，我们需要用另一个函数copy把新行复制到一个安全的位置。<br />
最后，我们需要在主函数main中控制getline和copy这两个函数。</p>
</li>
<li>
<p>main与getline之间通过一对参数及一个返回值进行数据交换。<br />
在getline函数中，两个参数是通过程序行</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getline</span><span class="params">(<span class="keyword">char</span> s[], <span class="keyword">int</span> lim)</span></span></span><br></pre></td></tr></table></figure>
<p>声明的，它把第一个参数s声明为数组，把第二个参数lim声明为整型。<br />
声明中包含提供数组大小的参数的目的是留出存储空间。在getline函数中没有必要指明数组s的长度，这是因为该数组的大小是在main函数中设置的。</p>
</li>
<li>
<p><strong>有些函数返回有用的值，而有些函数（如copy）仅用于执行一些动作，并不返回值。<br />
copy函数的返回值类型为void，它显式说明该函数不返回任何值</strong>。</p>
</li>
<li>
<p>getline函数把字符’\0’（即空字符，其值为0）插入到它创建的数组的末尾，以标记字符串的结束。<br />
<strong>这一约定已被C语言采用：当在C语言程序中出现类似于&quot;hello\0&quot;的字符串常量时，它将以字符数组的形式存储，数组的各元素分别存储字符串的各个字符，并以’\0’标志字符串的结束</strong>。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">h e l l o \n \0</span><br></pre></td></tr></table></figure>
<p><strong>printf函数中的格式规范%s规定，对应的参数必须是以这种形式表示的字符串</strong>。</p>
<p><strong>空字符’\0’不是普通文本的一部分</strong>。</p>
</li>
<li>
<p>值得一提的是，即使是上述这样很小的程序，在传递参数时也会遇到一些麻烦的设计问题。<br />
例如，当读入的行长度大于允许的最大值时，该如何处理——<br />
从getline函数的角度：调用getline函数的程序无法预先知道输入行的长度，因此getline函数需要检查是否溢出。<br />
从main函数的角度：getline函数的执行是安全的，无论是否到达换行符字符，当数组满时它将停止读字符。此时main函数可以通过测试行的长度以及检查返回的最后一个字符来判定当前行是否太长，然后再根据具体的情况处理。<br />
为了简化程序，我们在这里不考虑这个问题。</p>
</li>
</ol>
<h5 id="外部变量与作用域"><a class="markdownIt-Anchor" href="#外部变量与作用域"></a> 外部变量与作用域</h5>
<p><code>longest_line_2.c</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAXLINE 1000 	<span class="comment">/* maximum input line length */</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> max;				<span class="comment">/* maximum length seen so far */</span></span><br><span class="line"><span class="keyword">char</span> line[MAXLINE];		<span class="comment">/* current input line */</span></span><br><span class="line"><span class="keyword">char</span> longest[MAXLINE];	<span class="comment">/* longest line saved here */</span> </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getline</span><span class="params">(<span class="keyword">void</span>)</span></span>; </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">copy</span><span class="params">(<span class="keyword">void</span>)</span></span>; </span><br><span class="line"></span><br><span class="line"><span class="comment">/* print longest input line; specialized version */</span> </span><br><span class="line">main() </span><br><span class="line">&#123; </span><br><span class="line">	<span class="keyword">int</span> len; </span><br><span class="line">	<span class="keyword">extern</span> <span class="keyword">int</span> max; </span><br><span class="line">	<span class="keyword">extern</span> <span class="keyword">char</span> longest[]; </span><br><span class="line">    </span><br><span class="line">	max = <span class="number">0</span>; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">while</span> ((len = getline()) &gt; <span class="number">0</span>) </span><br><span class="line">		<span class="keyword">if</span> (len &gt; max) &#123; </span><br><span class="line">			max = len; </span><br><span class="line">			copy(); </span><br><span class="line">		&#125; </span><br><span class="line">	<span class="keyword">if</span> (max &gt; <span class="number">0</span>) 		<span class="comment">/* there was a line */</span> </span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, longest); </span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="comment">/* getline: specialized version */</span> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getline</span><span class="params">(<span class="keyword">void</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="keyword">int</span> c, i; </span><br><span class="line">	<span class="keyword">extern</span> <span class="keyword">char</span> line[]; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; MAXLINE - <span class="number">1</span> &amp;&amp; (c = getchar()) != EOF &amp;&amp; c != <span class="string">&#x27;\n&#x27;</span>; ++i) </span><br><span class="line">		line[i] = c; </span><br><span class="line">	<span class="keyword">if</span> (c == <span class="string">&#x27;\n&#x27;</span>) &#123; </span><br><span class="line">		line[i] = c; </span><br><span class="line">		++i; </span><br><span class="line">	&#125;  </span><br><span class="line">    line[i] = <span class="string">&#x27;\0&#x27;</span>; </span><br><span class="line">	<span class="keyword">return</span> i; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="comment">/* copy: specialized version */</span> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">copy</span><span class="params">(<span class="keyword">void</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="keyword">int</span> i; </span><br><span class="line">	<span class="keyword">extern</span> <span class="keyword">char</span> line[], longest[]; </span><br><span class="line">    </span><br><span class="line">	i = <span class="number">0</span>; </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">while</span> ((longest[i] = line[i]) != <span class="string">&#x27;\0&#x27;</span>) </span><br><span class="line">		++i; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>
<p><code>longest_line_1.c</code>中，main函数中的变量（如line、longest等）是main函数的<strong>私自变量或局部变量</strong>。由于<strong>它们在main函数中定义，且未在其它函数中声明，因此其它函数不能直接访问它们。函数间只能通过参数和返回值进行数据交换</strong>。<br />
其它函数中声明的变量也同样如此。例如，getline函数中声明的变量i与copy函数中声明的变量i没有关系。<br />
<strong>函数中的每个局部变量只在函数被调用时存在，在函数执行完毕退出时消失</strong>。这也是其它语言通常把这类变量称为<strong>自动变量</strong>的原因。以后我们使用“自动变量”代表“局部变量”。	<br />
由于自动变量只在函数调用执行期间存在，因此，<strong>在函数的两次调用之间，自动变量不保留前次调用时的赋值，且在每次进入函数时都要显式为其赋值。如果自动变量没有赋值，则其中存放的是无效值</strong>（C语言中有<strong>static存储类，这种类型的局部变量在多次函数调用之间保持值不变</strong>）。</p>
</li>
<li>
<p>除自动变量外，还可以<strong>定义位于所有函数外部的变量</strong>，也就是说，<strong>在所有函数中都可以通过变量名访问这种类型的变量</strong>。<br />
由于<strong>外部变量</strong>可以在<strong>全局范围</strong>内访问，因此，<strong>函数间可以通过外部变量交换数据，而不必使用参数表</strong>。<br />
再者，<strong>外部变量在程序执行期间一直存在</strong>，而不是在函数调用时产生、在函数执行完毕时消失。即使在对外部变量赋值的函数返回后，这些变量仍将保持原来的值不变。<br />
<strong>外部变量必须定义在所有函数之外，且只能定义一次，定义后编译程序将为它分配存储单元</strong>。<br />
<strong>在每个需要访问外部变量的函数中，必须声明相应的外部变量，此时说明其类型。声明时可以用<a href="https://www.geeksforgeeks.org/understanding-extern-keyword-in-c/">extern</a>语句显式声明，也可以通过上下文隐式声明</strong>。<br />
为了更详细地讨论外部变量，我们改写<code>character_array_1.c</code>，把<strong>line、longest</strong>与<strong>max</strong>声明成外部变量，这需要修改这3个函数的调用、声明与函数体。最终，我们得到<code>character_array_2.c</code>。</p>
</li>
<li>
<p><code>longest_line_2.c</code>中，前几行定义了main、getline与copy函数使用的几个外部变量<strong>line、longest</strong>与<strong>max</strong>，声明了各外部变量的类型，这样编译程序将为它们分配存储单元。<br />
从语法角度看，<strong>外部变量的定义与局部变量的定义是相同的，但由于它们位于各函数的外部，因此这些变量是外部变量</strong>。</p>
<p><strong>函数在使用外部变量之前，必须要知道外部变量的名字</strong>。<br />
要达到该目的，一种方式是<strong>在函数中使用extern类型的声明</strong>。<br />
这种类型的声明除了在前面加了一个关键字extern外，其它方面与普通变量的声明相同。<strong>某些情况下可以省略extern声明。在源文件中，如果外部变量的定义出现在使用它的函数之前，那么在那个函数中就没有必要使用extern声明。因此，main、getline及copy中的几个extern声明都是多余的</strong>。</p>
<p><strong>在通常的做法中，所有外部变量的定义都放在源文件的开始处，这样就可以省略extern声明</strong>。如果程序包含在多个源文件中，而某个变量在file1文件中定义、在file2和file3文件中使用，那么在文件file2与file3中就需要使用extern声明来建立该变量与其定义之间的联系。<br />
<strong>人们通常把变量和函数的extern声明放在一个单独的文件中（习惯上称之为头文件），并在每个源文件的开头使用#include语句把所要用的头文件包含进来。后缀名.h 约定为头文件名的扩展名</strong>。<br />
例如，标准库中的函数就是在类似于的头文件中声明的。</p>
</li>
<li>
<p><code>longest_line_2.c</code>中，由于getline与copy函数都不带参数，因此从逻辑上讲，在源文件开始处它们的原型应该是getline()与copy()。但为了与老版本的C语言程序兼容，ANSI C语言把空参数表看成老版本C语言的声明方式，并且对参数表不再进行任何检查。因此，<strong>在ANSI C中，如果要声明空参数表，则必须使用关键字void进行显式声明</strong>。<br />
读者应该注意到，这节中我们在谈论外部变量时谨慎地使用了<strong>定义（define）<strong>与</strong>声明（declaration）<strong>这两个词。</strong>“定义”表示创建变量或分配存储单元，而“声明”指的是说明变量的性质，但并不分配存储单元</strong>。<br />
顺便提一下，现在越来越多的人把用到的所有东西都作为外部变量使用，因为似乎这样可以简化数据的通信——参数表变短了，且在需要时总可以访问这些变量。但是，即使在不使用外部变量的时候，它们也是存在的。<strong>过分依赖外部变量会导致一定的风险，因为它会使程序中的数据关系模糊不清——外部变量的值可能会被意外地或不经意地修改，而程序的修改又变得十分困难</strong>。<br />
我们前面编写的<strong>打印最长文本行的程序的第2个版本就不如第1个版本好，原因有两方面：其一便是使用了外部变量；另一方面，第2个版本中的函数将它们所操纵的变量名直接写入了函数，从而使这两个有用的函数失去了通用性</strong>。</p>
</li>
</ol>
<p>到目前为止，我们已经对C语言的传统核心部分进行了介绍。借助于这些少量的语言元素，我们已经能够编写出相当规模的有用的程序。</p>
]]></content>
  </entry>
  <entry>
    <title>Markdown</title>
    <url>/2021/03/29/Markdown/</url>
    <content><![CDATA[<h4 id="标题"><a class="markdownIt-Anchor" href="#标题"></a> 标题</h4>
<h1 id="一级标题"><a class="markdownIt-Anchor" href="#一级标题"></a> 一级标题</h1>
<h2 id="二级标题"><a class="markdownIt-Anchor" href="#二级标题"></a> 二级标题</h2>
<h3 id="三级标题"><a class="markdownIt-Anchor" href="#三级标题"></a> 三级标题</h3>
<h4 id="四级标题"><a class="markdownIt-Anchor" href="#四级标题"></a> 四级标题</h4>
<h5 id="五级标题"><a class="markdownIt-Anchor" href="#五级标题"></a> 五级标题</h5>
<h6 id="六级标题"><a class="markdownIt-Anchor" href="#六级标题"></a> 六级标题</h6>
<a id="more"></a>
<br/>
<br/>
<br/>
<h4 id="段落"><a class="markdownIt-Anchor" href="#段落"></a> 段落</h4>
<p>现实总是令人遗憾的。<strong>（回车键：下一段，可在两段之间再添加空行，全凭个人喜好。&lt;br/&gt;：显式换行符号）</strong></p>
<p>我们当中太多人已经习惯于沿着那一成不变的“典型成功道路”前进：中考，高考，考研，出国……<strong>（shift+回车键：段内换行）</strong><br />
数不清的人们在埋头赶路。走在前面的已然迷失，跟在后面的却还未开始思考。这不应该是一个国家、一个时代的精神。</p>
<br/>
<br/>
<br/>
<h4 id="字体"><a class="markdownIt-Anchor" href="#字体"></a> 字体</h4>
<p><em>斜体文本</em></p>
<p><em>斜体文本</em></p>
<br/>
<p><strong>粗体文本</strong></p>
<p><strong>粗体文本</strong></p>
<br/>
<p><em><strong>粗斜体文本</strong></em></p>
<p><em><strong>粗斜体文本</strong></em></p>
<br/>
<br/>
<br/>
<h4 id="分隔线"><a class="markdownIt-Anchor" href="#分隔线"></a> 分隔线</h4>
<hr />
<hr />
<hr />
<p><strong>（三个及三个以上均可建立分隔线；行内不能有其他东西；符号间可任意插入空格）</strong></p>
<br/>
<br/>
<br/>
<h4 id="删除线"><a class="markdownIt-Anchor" href="#删除线"></a> 删除线</h4>
<p><s>删除线</s></p>
<br/>
<br/>
<br/>
<h4 id="下划线"><a class="markdownIt-Anchor" href="#下划线"></a> 下划线</h4>
<p><u>下划线</u><strong>（HTML标签）</strong></p>
<br/>
<br/>
<br/>
<h4 id="脚注"><a class="markdownIt-Anchor" href="#脚注"></a> 脚注</h4>
<p>创建脚注格式类似这样<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup></p>
<p>创建脚注格式类似这样<sup class="footnote-ref"><a href="#fn2" id="fnref2">[2]</a></sup></p>
<br/>
<br/>
<br/>
<h4 id="无序列表"><a class="markdownIt-Anchor" href="#无序列表"></a> 无序列表</h4>
<ul>
<li>第一项</li>
<li>第二项</li>
<li>第三项</li>
</ul>
<br/>
<ul>
<li>第一项</li>
<li>第二项</li>
<li>第三项</li>
</ul>
<br/>
<ul>
<li>第一项</li>
<li>第二项</li>
<li>第三项</li>
</ul>
<br/>
<br/>
<br/>
<h4 id="有序列表"><a class="markdownIt-Anchor" href="#有序列表"></a> 有序列表</h4>
<ol>
<li>第一项</li>
<li>第二项</li>
<li>第三项</li>
</ol>
<br/>
<br/>
<br/>
<h4 id="列表嵌套"><a class="markdownIt-Anchor" href="#列表嵌套"></a> 列表嵌套</h4>
<ul>
<li>第一项 <strong>（回车+退格）</strong>
<ul>
<li>第一项嵌套的第一个元素</li>
<li>第一项嵌套的第二个元素 <strong>（四次回车）</strong></li>
</ul>
</li>
</ul>
<br/>
<ol>
<li>
<p>第一项</p>
<ul>
<li>
<p>第一项嵌套的第一个元素</p>
</li>
<li>
<p>第一项嵌套的第二个元素</p>
</li>
</ul>
</li>
</ol>
<br/>
<ul>
<li>第一项
<ol>
<li>第一项嵌套的第一个元素</li>
<li>第一项嵌套的第二个元素</li>
</ol>
</li>
</ul>
<br/>
<ol>
<li>第一项
<ol>
<li>第一项嵌套的第一个元素</li>
<li>第一项嵌套的第二个元素</li>
</ol>
</li>
</ol>
<br/>
<br/>
<br/>
<h4 id="区块"><a class="markdownIt-Anchor" href="#区块"></a> 区块</h4>
<blockquote>
<p>区块引用</p>
</blockquote>
<br/>
<blockquote>
<p>区块最外层</p>
<blockquote>
<p>第一层嵌套</p>
<blockquote>
<p>第二层嵌套</p>
<blockquote>
<p>…</p>
</blockquote>
</blockquote>
</blockquote>
</blockquote>
<br/>
<blockquote>
<p>区块中使用列表</p>
<ul>
<li>第一项</li>
<li>第二项</li>
<li>…</li>
</ul>
</blockquote>
<br/>
<ul>
<li>
<p>列表中使用区块</p>
<blockquote>
<p>区块</p>
</blockquote>
</li>
</ul>
<br/>
<br/>
<br/>
<h4 id="代码"><a class="markdownIt-Anchor" href="#代码"></a> 代码</h4>
<p><code>code</code></p>
<br/>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br></pre></td></tr></table></figure>
<p><strong>（code block；语言可不指定；按 ↓ 键跳出代码块继续创作）</strong></p>
<br/>
<br/>
<br/>
<h4 id="链接"><a class="markdownIt-Anchor" href="#链接"></a> 链接</h4>
<p><a href="https://primer2019.github.io/">我的博客（链接名称）</a></p>
<br/>
<p><a href="https://primer2019.github.io/">https://primer2019.github.io/</a></p>
<br/>
<p>这个链接用1作为网址变量 <a href="https://www.google.com/">Google</a></p>
<p>这个链接用2作为网址变量 <a href="https://github.com/">GitHub</a></p>
<p>（用这种方式可以在文档结尾统一为变量赋值）</p>
<br/>
<br/>
<br/>
<h4 id="图片"><a class="markdownIt-Anchor" href="#图片"></a> 图片</h4>
<p><img src="/images/wechat.png" alt="alt wechat" title="wechat" /></p>
<p><strong>（小括号中的&quot;图片地址&quot;为 本地位置 或 网址 均可。）</strong></p>
<br/>
<p><a href="http://static.runoob.com/images/runoob-logo.png">RUNOOB</a></p>
<p><strong>（为变量赋值只能用 网址 。）</strong></p>
<br/>
<img src="http://static.runoob.com/images/runoob-logo.png" width="200％">
<p><strong>（markdown没有办法指定图片的高度与宽度，有需要的话，可直接使用HTML的&lt;img&gt;标签）</strong></p>
<br/>
<br/>
<br/>
<h4 id="表格"><a class="markdownIt-Anchor" href="#表格"></a> 表格</h4>
<table>
<thead>
<tr>
<th style="text-align:left">表头</th>
<th style="text-align:center">表头</th>
<th style="text-align:right">表头</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">单元格</td>
<td style="text-align:center">单元格</td>
<td style="text-align:right">单元格</td>
</tr>
</tbody>
</table>
<br/>
<br/>
<br/>
<h4 id="注"><a class="markdownIt-Anchor" href="#注"></a> 注</h4>
<ul>
<li>
<p><strong>用markdown难以实现的功能，可以直接用HTML撰写。</strong></p>
</li>
<li>
<p><strong>markdown使用了很多特殊符号表示特定的意义，因此如果要显示这些特定的符号，需要使用转义字符。markdown使用 \ 转义特殊字符。</strong></p>
<p>\   `   *   _   {}   []   ()   #   +   -   .   !</p>
</li>
<li>
<p><strong>可以使用两个美元符 $$ 包裹 TeX 或 LaTeX 格式的数学公式来实现数学公式的插入。</strong></p>
</li>
</ul>
<p><span class="katex-display"><span class="katex"><span class="katex-mathml"><math><semantics><mtable rowspacing="0.15999999999999992em" columnspacing="1em"><mtr><mtd><mstyle scriptlevel="0" displaystyle="false"><mn>1</mn></mstyle></mtd><mtd><mstyle scriptlevel="0" displaystyle="false"><mn>2</mn></mstyle></mtd><mtd><mstyle scriptlevel="0" displaystyle="false"><mn>3</mn></mstyle></mtd></mtr><mtr><mtd><mstyle scriptlevel="0" displaystyle="false"><mn>4</mn></mstyle></mtd><mtd><mstyle scriptlevel="0" displaystyle="false"><mn>5</mn></mstyle></mtd><mtd><mstyle scriptlevel="0" displaystyle="false"><mn>6</mn></mstyle></mtd></mtr><mtr><mtd><mstyle scriptlevel="0" displaystyle="false"><mn>7</mn></mstyle></mtd><mtd><mstyle scriptlevel="0" displaystyle="false"><mn>8</mn></mstyle></mtd><mtd><mstyle scriptlevel="0" displaystyle="false"><mn>9</mn></mstyle></mtd></mtr></mtable><annotation encoding="application/x-tex">\begin{matrix}
   1 &amp; 2 &amp; 3 \\
   4 &amp; 5 &amp; 6 \\
   7 &amp; 8 &amp; 9
  \end{matrix}
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:3.6000000000000005em;vertical-align:-1.5500000000000007em;"></span><span class="mord"><span class="mtable"><span class="col-align-c"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:2.05em;"><span style="top:-4.21em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord">1</span></span></span><span style="top:-3.0099999999999993em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord">4</span></span></span><span style="top:-1.8099999999999994em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord">7</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:1.5500000000000007em;"><span></span></span></span></span></span><span class="arraycolsep" style="width:0.5em;"></span><span class="arraycolsep" style="width:0.5em;"></span><span class="col-align-c"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:2.05em;"><span style="top:-4.21em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord">2</span></span></span><span style="top:-3.0099999999999993em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord">5</span></span></span><span style="top:-1.8099999999999994em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord">8</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:1.5500000000000007em;"><span></span></span></span></span></span><span class="arraycolsep" style="width:0.5em;"></span><span class="arraycolsep" style="width:0.5em;"></span><span class="col-align-c"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:2.05em;"><span style="top:-4.21em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord">3</span></span></span><span style="top:-3.0099999999999993em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord">6</span></span></span><span style="top:-1.8099999999999994em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord">9</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:1.5500000000000007em;"><span></span></span></span></span></span></span></span></span></span></span></span></p>
<hr class="footnotes-sep" />
<section class="footnotes">
<ol class="footnotes-list">
<li id="fn1" class="footnote-item"><p>Independence <a href="#fnref1" class="footnote-backref">↩︎</a></p>
</li>
<li id="fn2" class="footnote-item"><p>Freedom <a href="#fnref2" class="footnote-backref">↩︎</a></p>
</li>
</ol>
</section>
]]></content>
  </entry>
  <entry>
    <title>HelloBlog</title>
    <url>/2021/03/28/HelloBlog/</url>
    <content><![CDATA[<p><strong>博客之旅，正式启程。</strong></p>
<p><strong>Blog tour, officially set off.</strong></p>
<a id="more"></a>
]]></content>
  </entry>
</search>
